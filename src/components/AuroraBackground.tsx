/**
 * Flat design: the drifting aurora background has been removed.
 * Kept as a no-op so existing imports/usages stay valid.
 */
export function AuroraBackground(_props: {
  className?: string;
  intensity?: "soft" | "medium" | "bold";
}) {
  return null;
}

export default AuroraBackground;

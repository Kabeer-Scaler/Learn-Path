/**
 * Flat design: the floating glowing orbs have been removed.
 * Kept as a no-op so existing imports/usages stay valid.
 */
export function NeuralOrbs(_props: { count?: number; className?: string }) {
  return null;
}

export default NeuralOrbs;
